<h4>Access log:</h4>
<table class="table table-bordered table-condensed table-striped">
	<thead>
		<th>date</th>
		<th>member</th>
		<th>type</th>
		<th>plan</th>
		<th>denied access</th>
	</thead>
	<tbody>
	<% @logs.each do |log| %>
		<tr>
			<td><%= log.created_at.strftime("%m/%d/%Y %l:%M %P") %></td>
			<td><%= log.member_name %></td>
			<td><%= log.member_type %></td>
			<td><%= log.billing_plan %></td>
			<td><%= log.msg %></td>
		</tr>
	<% end %>
	</tbody>
</table>
